model User {
  id        String    @id @default(cuid())
  email     String    @unique
  name      String?
  password  String
  jobs      Job[]
  createdAt DateTime  @default(now())
  updatedAt DateTime  @updatedAt
}
